import styled from "styled-components";

export const Container = styled.div`
	background-color: lightGray;
	width: 17em;
	margin-bottom: 0.8em;
	display: flex;
	:nth-child(even) {
		margin-left: auto;
	}
`;
